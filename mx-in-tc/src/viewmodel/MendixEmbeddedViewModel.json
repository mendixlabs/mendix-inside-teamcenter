{
  "schemaVersion": "1.0.0",
  "ctx": {
    "selected": null
  },
  "data": {
    "containerRef": {
      "current": null
    },
    "error": null
  },
  "actions": {
    "loadMendix": {
      "actionType": "JSFunctionAsync",
      "method": "loadMendix",
      "deps": "js/mendixEmbeddedService",
      "inputData": {
        "containerRef": "{{data.containerRef}}",
        "config": "{{props.config}}",
        "subPanelContext": "{{props.subPanelContext}}",
        "context": "{{ctx}}",
        "reloadAction": "{{actions.loadMendix}}",
        "dispatch": "{{data.dispatch}}"
      }
    },
    "cleanup": {
      "actionType": "JSFunction",
      "method": "mendixCleanupFunction",
      "deps": "js/mendixEmbeddedService",
      "inputData": {
        "containerRef": "{{data.containerRef}}"
      }
    },
    "reload": {
      "actionType": "JSFunction",
      "method": "reloadPage",
      "deps": "js/mendixEmbeddedService"
    }
  },
  "conditions": {
    "isPopupBlocked": {
      "expression": "data.error && data.error.code === 'POPUP_BLOCKED'"
    },
    "hasGeneralError": {
      "expression": "data.error && data.error.code !== 'POPUP_BLOCKED'"
    }
  },
  "lifecycleHooks": {
    "onUpdate": [
      {
        "action": "loadMendix",
        "observers": [
          "props.config",
          "props.subPanelContext.declarativeKeyContext",
          "viewModel.ctx.selected"
        ]
      }
    ],
    "onUnmount": "cleanup"
  },
  "onEvent": [],
  "i18n": {},
  "messages": {},
  "props": {
    "config": {
      "type": "string"
    },
    "subPanelContext": {
      "type": "object"
    }
  }
}